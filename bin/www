#!/usr/bin/env node

const server = require('../src/server')

const { PORT, NODE_ENV } = process.env

const port = PORT || 8085
server.listen(port, () => {
  console.log(`server listening on port ${port} in ${NODE_ENV || 'development'} mode`)
})
